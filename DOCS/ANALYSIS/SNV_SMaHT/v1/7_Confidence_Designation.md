# Confidence Designation

After donor-level refinement, candidate variants are assigned a final confidence designation based on the level of supporting evidence observed across sequencing technologies, variant callers, and tissues.

The final confidence designation summarizes the validation results obtained during the pipeline and provides an overall assessment of the likelihood that a variant represents a true somatic mosaic event.

Confidence labels are recorded in the `FILTER` field of the final VCF file.

## Confidence Classification

Variants are classified into three confidence levels:

### HighConf

Variants of the highest confidence.

Variants are labeled `HighConf` when they satisfy one of the following conditions:

- `CrossTech`
- `CrossTissue` **and** `CrossCaller`

These variants have independent evidence across multiple sequencing technologies or strong supporting evidence across tissues and variant callers.

### LowConf

Variants of lower confidence that still show partial supporting evidence.

Variants are labeled `LowConf` when they satisfy one of the following conditions:

- `CrossTissue`
- `CrossCaller`

These variants have supporting evidence from either multiple tissues or multiple variant callers but lack cross-technology confirmation.

### LikelyArtifact

Variants of the lowest confidence.

Variants are labeled `LikelyArtifact` when:

- no cross-evidence tags are present (`CrossTech`, `CrossTissue`, or `CrossCaller`)

These variants pass all upstream filters but lack independent supporting evidence. In most cases they represent sequencing or alignment artifacts, although some may represent true mosaic variants in samples with limited long-read coverage.

## Interpretation

The confidence designation provides a simplified summary of the evidence supporting each variant while preserving the detailed annotations generated during the pipeline.

Users may apply additional filtering strategies depending on the desired balance between sensitivity and specificity.

## Source Code

All the relevant code can be accessed in the GitHub repository:

- [set_filter_vcf.py](https://github.com/smaht-dac/calling-pipelines/blob/main/dockerfiles/calling_utils/scripts/set_filter_vcf.py) [set_filter_vcf]

---

<!-- This section relies on the html links generated by GitHub Pages 
and will not render correctly in Markdown -->
<div style="text-align: right">
    <a href="/pipelines-docs/"> Home </a> -
    <a href="0_Overview.html"> Overview </a> -
    <a href="1_Short_Read_Calling.html"> Short-Read Calling </a> -
    <a href="2_Long_Read_Calling.html"> Long-Read Calling </a> -
    <a href="3_Calls_Merging.html"> Calls Merging </a> -
    <a href="4_Filtering.html"> Filtering </a> -
    <a href="5_Cross_Validation.html"> Cross-Technology Validation </a> -
    <a href="6_Donor_Level_Refinement.html"> Donor-Level Refinement </a> -
    <a> <b> Confidence Designation </b> </a>
</div>